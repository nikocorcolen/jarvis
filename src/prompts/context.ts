/**
 * Renders the prompt that wraps a steering+spec dump for handing off
 * full context to an AI agent in a new session.
 *
 * Stub. Real content lives in the `context-dump` spec.
 */
export function renderContextPrompt(_payload: {
  steering: string;
  spec: { name: string; body: string } | null;
}): string {
  return `[TODO: context dump prompt]`;
}
